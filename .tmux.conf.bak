#
# Previous tmux config, personal edit 
#
#
#
# Force 256 color, vim color fix
set -g default-terminal "xterm-256color"

# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# mouse scrolling??
set -g mouse on

# Vim style pane navigation
#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R

# Use alt-vim keys without prefix to switch panes
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Split windows using alt | and - with same above method
bind -n M-| split-window -h
bind -n M-- split-window -v

# quiet
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# Status bar
# set -g status-fg white
# set -g status-bg blue
# set -g status-left ''
# set -g status-right-length 60

set -g pane-border-style "fg=blue"
set -g pane-active-border-style "bg=black fg=blue"

set -g status-bg blue
set -g status-fg white
set -g status-interval 60
set -g status-left-length 30
#set -g status-left '#[fg=green](#S) #(whoami)
set -g status-right '#[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white]%H:%M#[default]'

# Stop esc key from listening for switch pan commands, annoying
set -s escape-time 0
